import { createComponent } from '../../utils/create-component.js';
import { MdNavigationRail } from './md-navigation-rail.js';

export const NavigationRail = createComponent<MdNavigationRail>({
  tagName: 'md-navigation-rail',
  elementClass: MdNavigationRail,
});
